$creds = (get-storedcredential -ascredentialobject).TargetName | Out-String -Stream | Select-String -Pattern "LegacyGeneric:target=Adobe"
foreach($cred in $creds) { remove-storedcredential -Target $cred }
